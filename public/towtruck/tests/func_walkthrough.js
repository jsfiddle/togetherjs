// =SECTION Setup

Test.require("ui", "chat", "util", "session", "jquery", "storage", "peers", "cursor", "windowing");
// => Loaded modules: ...

printChained(
  Test.resetSettings(),
  Test.startTowTruck());

// => ...
