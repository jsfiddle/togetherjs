/**
 * This file contains a global variable to mock bits of the TogetherJS to allow testing using Jasmine.
 * A useful refactor would be to remove the dependency on a global TJS object and instead inject it where it's
 * required in other files.
 */
var TogetherJS = {};


