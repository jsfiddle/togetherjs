<% /*
  This is used to show the help when you type /help.  Used in
  TogetherJS.localChatMessage().

*/ %>
/help : {{ gettext( "this message" ) }}
/test : {{ gettext( "run an automated/randomized test (or stop one that is in progress)" ) }}
  /test start N : {{ gettext( "run N times (instead of default 100)" ) }}
  /test show : {{ gettext( "show what kind of actions the random test would take (or stop showing)" ) }}
  /test describe : {{ gettext( "describe the possible actions (instead of showing them)" ) }}
/clear : {{ gettext( "clear the chat area" ) }}
/record : {{ gettext( "open up a recorder for the session" )}}
/playback URL : {{ gettext( "play back a session that was recorded (it's up to you to figure out how to host it)" ) }}
  /playback local:NAME : {{ gettext( "play a locally saved log" ) }}
/savelogs NAME : {{ gettext( "save the currently recorded logs under NAME (recorder must be open)" ) }}
/baseurl : {{ gettext( "set a local baseUrl to load TogetherJS from, for debugging a development version of TogetherJS." ) }}
/config : {{ gettext( "override some TogetherJS configuration parameters") }}
  /config VAR VALUE : {{ gettext( "set TogetherJS.config(\"VAR\", VALUE).  VALUE must be a legal Javascript/JSON literal." ) }}
  /config clear : {{ gettext( "remove all overridden configuration" ) }}
