<% /*

  This is used to show the help when you type /help.  Used in
  TowTruck.localChatMessage().  Variables:

  * tabIndependent: true if tab independence is turned on

*/ %>
/help : this message
/tab : turn tab independence on/off (allows two tabs to have different identities)
  <% if (tabIndependent) { %>tab independence on<% } else { %>tab independence off (the default)<% } %>
/test : run an automated/randomized test (or stop one that is in progress)
  /test show : show what kind of actions the random test would take (or stop showing)
/clear : clear the chat area
